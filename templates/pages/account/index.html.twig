{% extends 'base.html.twig' %}

{% block title %} Аккаунт {% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12 title_header">
            <h3> Информация о профиле </h3>

            {% include 'includes/flash_messages.html.twig' %}
            {% endfor %}

        </div>

        <div class="col-md-12">
            <form action="{{ url('route.account.update') }}" method="POST">

                <div class="form-group row">
                    <label class="col-md-3 col-form-label font-weight-bold text-right" for="email">Email</label>
                    <div class="col-md-7">
                        <input class="form-control" type="email" name="email" id="email"
                               value="{{ app.user.getEmail() }}" autocomplete="email">
                    </div>
                </div>


                <div class="form-group row">
                    <label class="col-md-3 col-form-label font-weight-bold text-right"
                           for="first_name">Имя</label>
                    <div class="col-md-7">
                        <input class="form-control" type="text" value="{{ app.user.getFirstName() }}"
                               placeholder="Olususi" name="first_name" id="first_name"
                               autocomplete="first_name">
                    </div>
                </div>

                <div class="form-group row">
                    <label class="col-md-3 col-form-label font-weight-bold text-right"
                           for="last_name">Фамилия</label>
                    <div class="col-md-7">
                        <input class="form-control" type="text" value="{{ app.user.getLastName() }}"
                               placeholder="Oluyemi" name="last_name" id="last_name" autocomplete="last_name">
                    </div>
                </div>

                <div class="form-group row">
                    <label class="col-md-3 col-form-label font-weight-bold text-right"
                           for="location">Геолокация</label>
                    <div class="col-md-7">
                        <input class="form-control" type="text" value="{{ app.user.getLocation() }}"
                               placeholder="Lagos, Nigeria." name="location" id="location" autocomplete="">
                    </div>
                </div>
                <div class="form-group row">
                    <label class="col-md-3 col-form-label font-weight-bold text-right" for="website">Веб
                        сайтe</label>
                    <div class="col-md-7">
                        <input class="form-control" type="text" value="{{ app.user.getWebsite() }}"
                               placeholder="https://example.com" name="website" id="website"
                               autocomplete="url">
                    </div>
                </div>

                <div class="form-group">
                    <div class="offset-sm-3 col-md-7 pl-2">
                        <button class="btn btn btn-outline-dark" type="submit">
                            <i class="fas fa-user-edit fa-sm"></i>&nbsp;&nbsp;Обновить профиль
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <div class="row">

        <div class="col-md-12 title_header">
            <h3> Изменить пароль </h3>
        </div>

        <div class="col-md-12">
            <form action="{{ url('route.account.change_password') }}" method="POST">
                <div class="form-group row">
                    <label class="col-md-3 col-form-label font-weight-bold text-right" for="password">Новый
                        пароль</label>
                    <div class="col-md-7">
                        <input class="form-control" type="password" name="password" id="password"
                               autocomplete="new-password" aria-autocomplete="list">
                    </div>
                </div>
                <div class="form-group row">
                    <label class="col-md-3 col-form-label font-weight-bold text-right" for="confirmPassword">Повторите
                        пароль</label>
                    <div class="col-md-7">
                        <input class="form-control" type="password" name="confirmPassword" id="confirmPassword"
                               autocomplete="new-password">
                    </div>
                </div>
                <div class="form-group">
                    <div class="offset-sm-3 col-md-7 pl-2">
                        <button class="btn btn-outline-dark" type="submit">
                            <i class="fa fa-lock fa-sm"></i>&nbsp;&nbsp;Изменить пароль
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>


    <div class="row">
        <div class="col-md-12 title_header">
            <h3> Удалить аккаунт</h3>
        </div>

        <div class="col-md-12">
            <form action="{{ url('route.account.delete') }}" method="POST"
                  onsubmit="return confirm('Are you sure you want to delete your account?');">
                <div class="form-group">
                    <p class="offset-sm-3 col-md-7 pl-2">Вы можете удалить свой аккаунт. Но имейте в виду, что
                        <b>это действие необратимо!</b></p>
                    <div class="offset-sm-3 col-md-7 pl-2">
                        <button class="btn btn-outline-danger" type="submit">
                            <i class="fa fa-trash fa-sm"></i>&nbsp;&nbsp;Удалить мой аккаунт
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>
{% endblock %}
